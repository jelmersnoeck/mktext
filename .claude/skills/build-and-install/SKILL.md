---
name: build-and-install
description: Build the mktext app bundle in release mode and launch it
---

Build and install mktext by running the build script, then open the app:

1. Run `scripts/build.sh --release` from the project root
2. Run `open mktext.app` to launch it

If the build fails, show the error output and suggest fixes.
